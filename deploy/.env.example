# ============================================================
# deploy/.env — конфигурация запуска FixByte CRM через Docker
# Скопируйте в deploy/.env и отредактируйте:
#   cp .env.example .env
# ============================================================

# ── Учётные данные (ОБЯЗАТЕЛЬНО смените перед боевым запуском) ──
MARIADB_PASSWORD=changeme

FIXBYTE_ADMIN_USERNAME=admin
FIXBYTE_ADMIN_PASSWORD=changeme

FIXBYTE_OPERATOR_USERNAME=operator
FIXBYTE_OPERATOR_PASSWORD=changeme

# ── Обычный режим (bridge, docker-compose.yml) ────────────
# На каком порту хоста слушать веб-интерфейс:
HOST_HTTP_PORT=9087

# ── Реквизиты компании (шапка чека и акта приёмки) ────────
# Стартовые значения; дальше правятся в панели «Настройки чека».
# Кириллицу лучше вписывать здесь (в .env), а не в application.properties.
FIXBYTE_COMPANY_NAME=FixByte
FIXBYTE_COMPANY_SUBTITLE=
FIXBYTE_COMPANY_ADDRESS=
FIXBYTE_COMPANY_PHONE=
FIXBYTE_COMPANY_EMAIL=

# ── Напоминания ──────────────────────────────────────────
# Через сколько дней «готовый, но не забранный» заказ попадает в напоминания.
FIXBYTE_STALE_ORDER_DAYS=30

# ── Хранилище полных бэкапов ──────────────────────────────
# Контейнер видит РЕАЛЬНЫЙ /mnt Linux-хоста (docker-compose монтирует
# /mnt:/mnt) — там же обычно точки монтирования NAS/USB и т.п. Конкретную
# папку внутри /mnt можно выбрать прямо в панели администратора («База
# данных» → «Хранилище полных бэкапов», кнопка «Обзор» показывает реальное
# содержимое хоста) — переменная ниже нужна только как каталог по
# умолчанию / обратная совместимость. Пусто и ничего не выбрано в панели =
# хранилище выключено (доступно только скачивание в браузер).
FIXBYTE_BACKUP_DIR=/mnt
# Сколько последних полных бэкапов держать (старые удаляются). 0 = не удалять.
FIXBYTE_BACKUP_KEEP=14
# Периодичность планового полного бэкапа задаётся в панели администратора
# («База данных» → Автоматический бэкап), переменной окружения для этого нет.
# Не используется по умолчанию (весь /mnt хоста монтируется напрямую, см.
# docker-compose.yml). Нужна только если вместо всего /mnt хотите одну
# конкретную папку хоста — тогда в docker-compose.yml замените volume
# "/mnt:/mnt" на "${BACKUP_HOST_DIR:-./backups}:/mnt".
BACKUP_HOST_DIR=./backups

# ── Режим macvlan (docker-compose.macvlan.yml, только Linux) ──
# Контейнер получает свой IP в вашей локальной сети.
# Родительский сетевой интерфейс Linux-хоста (ip -o link show): eth0 / enp3s0 / eno1 ...
MACVLAN_PARENT=eth0
# Параметры вашей LAN (как на роутере):
LAN_SUBNET=192.168.1.0/24
LAN_GATEWAY=192.168.1.1
# Блок адресов, который роутер НЕ раздаёт по DHCP:
LAN_IP_RANGE=192.168.1.240/28
# Фиксированный IP контейнера CRM — по нему заходят телефоны:  http://192.168.1.242:9087
CRM_IP=192.168.1.242
# IP для доступа самого Docker-хоста к контейнеру (см. macvlan/host-shim.sh):
HOST_SHIM_IP=192.168.1.241
